/*print("ye")
hook.Add("PopulatePropMenu", "rlr_showblocklvl", function()
    print("called hook")
end)

local function handle(strFilename, strName, tabContents, icon, id, parentid, needsapp)
    print(strFilename, strName, tabContents, icon, id, parentid, needsapp)
end

spawnmenu.PopulateFromTextFiles( handle )
*/
